# Flask
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY="matrix"

# Database (SQLite path by default)
DATABASE_URL=sqlite:///reco.db

# LLM configuration (Grok or OpenAI-compatible endpoint)
LLM_MODEL="moonshotai/kimi-k2:free"
LLM_API_KEY="sk-*****************************************************************045"
# If your provider is OpenAI-compatible, you can set a custom base. Leave blank to use default.
LLM_API_BASE="https://openrouter.ai/api/v1"

# Embeddings (choose one strategy)
# If you prefer local embeddings (no API): sentence-transformers model name
EMBEDDINGS_MODEL=sentence-transformers/all-MiniLM-L6-v2

# If you prefer OpenAI-compatible embeddings instead, set:
# EMBEDDINGS_PROVIDER=openai
# EMBEDDINGS_MODEL=text-embedding-3-small
# and ensure LLM_API_KEY/LLM_API_BASE are set appropriately.
